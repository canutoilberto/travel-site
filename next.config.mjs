/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "plus.unsplash.com",
      "i.pravatar.cc",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
